# Reverse Words

Write a function `reverse_words(sentence)` that takes a string, `sentence`, and **returns** a new string with the words in the opposite order.

For example, `reverse_words('kia ora koutou')` should **return** `'koutou ora kia'`.

*Hint: the* `str.split()` *method breaks a sentence up into a list of words.*
