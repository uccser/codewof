# Average Temperature

Write a function `average_temperature(temperatures)` that takes a Python list of numbers, `temperatures`, and **returns** the average of those temperatures.

Your function will not be tested with an empty list.

*Hint: the built in functions `sum()` and `len()` may be useful.*
