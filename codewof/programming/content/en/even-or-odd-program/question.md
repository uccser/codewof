# Even or Odd Program

Write a program that asks for a whole number and **prints** `Even` if the number is even, or `Odd` if the number is odd.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is even.*
