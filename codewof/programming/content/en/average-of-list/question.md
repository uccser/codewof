# Average of List

Build a function `average(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the average of those numbers.

The average is the sum of the numbers divided by how many numbers there are.
Your function will not be tested with an empty list.

*The simplest solution isn't an option for this question!*
