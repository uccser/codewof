# Print Each Word

Write a program that asks the user for a sentence, then **prints** each word of the sentence on a new line.

Words are separated by spaces.

*Hint: the* `str.split()` *method breaks a sentence up into a list of words.*
