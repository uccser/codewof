# Monogram

Write a function `monogram(first, last)` that takes a person's first name and last name, and **returns** their initials in UPPERCASE with no spaces or full stops.

For example, `monogram('aroha', 'kepa')` should **return** `'AK'`.

*Hint: the string method* `str.upper()` *and string indexing may be useful here.*
