# Shout Name

Write a program that asks the user for their name, then **prints** their name in UPPERCASE followed by an exclamation mark `!`.

*Hint: the string method* `str.upper()` *may be useful.*
