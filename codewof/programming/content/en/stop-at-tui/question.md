# Stop at Tui

Write a program that repeatedly asks the user which bird they can see.

Each time the bird is not a `tui`, your program should **print** `Keep looking`.
Once the user enters `tui`, your program should **print** `You spotted a tui!` and stop.

*Hint: this question is case sensitive, so `TUI` is not the same as `tui`.*
