# Double Letters

Write a function `double_letters(text)` that takes a string, `text`, and **returns** a new string where every character appears twice in a row.

For example, `double_letters('kia')` should **return** `'kkiiaa'`.
