# Sum of Squares

Write a function `sum_of_squares(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the sum of the squares of those numbers.

For example, `sum_of_squares([1, 2, 3])` should **return** `14` because 1 + 4 + 9 = 14.
