# Is Multiple?

Write a function `is_multiple(number, divisor)` that **returns** `True` if `number` is a multiple of `divisor`, or `False` otherwise.

*Hint: the modulo operator `%` is useful for finding out if a number divides evenly into another number.*
