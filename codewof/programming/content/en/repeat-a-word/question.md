# Repeat a Word

Write a program that asks for a word, then asks for a whole number of times to repeat it.

Your program should **print** the word repeated that many times, all on one line with no spaces.

For example, if the user enters `ka` and then `3`, your program should **print** `kakaka`.
