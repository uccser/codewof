# Count Longer Than

Write a function `count_longer_than(words, length)` that takes a Python list of strings, `words`, and a number, `length`, and **returns** how many of the strings are longer than `length` characters.
