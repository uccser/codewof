# Count Evens

Write a function `count_evens(numbers)` that takes a Python list of numbers, `numbers`, and **returns** how many of them are even.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is even.*
