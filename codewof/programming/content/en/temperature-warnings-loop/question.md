# Temperature Warnings

Write a program that repeatedly asks for a temperature in degrees celsius.

For each temperature, **print** `Hot!` if it is above 30, otherwise **print** `Fine`.

The program should stop when the user enters `0`.
