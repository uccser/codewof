# Count Words

Write a function `count_words(sentence)` that takes a string `sentence` and **returns** the number of words in it.

*Hint: the* `str.split()` *method breaks a sentence up into a list of words.*
