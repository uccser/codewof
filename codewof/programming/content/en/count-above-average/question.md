# Count Above Average

Write a function `count_above_average(numbers)` that takes a Python list of numbers, `numbers`, and **returns** how many of the numbers are greater than the average of the list.

Your function will not be tested with an empty list.

*Hint: the built in functions `sum()` and `len()` may come in handy.*
