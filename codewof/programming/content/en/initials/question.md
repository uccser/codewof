# Initials

Write a function `initials(first_name, last_name)` that takes two strings and **returns** the person's initials in UPPERCASE, with a full stop after each one.

For example, `initials("john", "smith")` should **return** `"J.S."`
