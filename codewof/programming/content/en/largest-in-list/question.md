# Largest in List

Build a function `largest(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the largest number in the list.

Your function will not be tested with an empty list.

*The simplest solution isn't an option for this question!*
