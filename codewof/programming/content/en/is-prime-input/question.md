# Is It Prime?

Write a program that asks for a whole number and **prints** `Prime` if the number is a prime number, or `Not prime` otherwise.

A prime number is greater than 1 and can only be divided evenly by 1 and itself.

*Hint: the `%` (modulo) operator is useful for checking divisors.*
