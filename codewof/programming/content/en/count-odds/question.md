# Count Odds

Write a function `count_odds(numbers)` that takes a Python list of numbers, `numbers`, and **returns** how many of them are odd.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is odd.*
