# Digit Sum

Write a function `digit_sum(number)` that takes a positive integer and **returns** the sum of its digits.

For example, `digit_sum(123)` should **return** `6` because 1 + 2 + 3 = 6.

*Hint: `str(number)` turns a number into a string that you can loop over.*
