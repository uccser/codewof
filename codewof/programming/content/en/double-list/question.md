# Double List

Write a function `double_list(numbers)` that takes a Python list of numbers, `numbers`, and **returns** a new list where every number has been doubled.

For example, `double_list([1, 2, 3])` should **return** `[2, 4, 6]`.
