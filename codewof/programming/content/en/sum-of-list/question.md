# Sum of a List

Write a function `total(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the sum of all the numbers in the list.

*The simplest solution isn't an option for this question - you may not use the built in `sum()` function!*
