# Letter Count

Write a function `letter_count(word, letter)` that takes two strings and **returns** the number of times `letter` appears in `word`.

*Hint: the string method `str.count()` may be useful.*
