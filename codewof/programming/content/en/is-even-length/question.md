# Is Even Length

Write a function `is_even_length(word)` that takes a string, `word`, and **returns** `True` if the string has an even number of characters, or `False` otherwise.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is even.*
