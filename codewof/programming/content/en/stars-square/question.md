# Stars Square

Write a function `stars_square(size)` that **prints** a square of `#` symbols with `size` rows and `size` columns.

If `size` is 0 or less, your function should **print** `Too small!`
