# Stars Square

Write a function `square(size)` that takes an integer, `size`, and **prints** a square of `#` symbols with the given width and height.

For example, `square(3)` should **print**:

```
###
###
###
```

If `size` is 0 or less, nothing should be printed.
