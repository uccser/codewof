# Coldest Temperature

Build a function `coldest(temperatures)` that takes a Python list of numbers, `temperatures`, and **returns** the lowest temperature in the list.

Your function will not be tested with an empty list.

*The simplest solution isn't an option for this question!*
