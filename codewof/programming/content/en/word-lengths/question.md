# Word Lengths

Write a function `word_lengths(words)` that takes a Python list of strings, `words`, and **returns** a new list containing the length of each string.

For example, `word_lengths(['kiwi', 'moa'])` should **return** `[4, 3]`.
