# Is In Range

Write a function `is_in_range(number, low, high)` that **returns** `True` if `number` is between `low` and `high` (inclusive), or `False` otherwise.

You can assume `low` is always less than or equal to `high`.
