# Seconds to Minutes

Write a function `print_time(total_seconds)` that takes a number of seconds and **prints** it in the format `{minutes} minutes and {seconds} seconds`.

For example, `print_time(90)` should **print** `1 minutes and 30 seconds`.

*Hint: the `//` (floor division) and `%` (modulo) operators are useful here.*
