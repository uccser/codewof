# Triangle Area

Write a function `triangle_area(base, height)` that **returns** the area of a triangle with the given base and height.

The formula for the area of a triangle is `area = base * height / 2`.
