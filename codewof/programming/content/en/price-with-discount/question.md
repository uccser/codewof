# Price With Discount

Write a function `discounted_price(price, percent)` that takes a price and a discount percentage, and **returns** the price after the discount has been taken off.

For example, `discounted_price(100, 10)` should **return** `90.0`.
