# Print Score Message

Write a function `print_score_message(name, score)` that takes a player's name and their score, and **prints** `{name} scored {score} points`.

*Hint: the `str` function might be useful, e.g. `str(12)` is "12"*
