# Net Pay

Write a function `net_pay(hours, rate)` that takes the number of hours worked and the hourly pay rate in dollars, and **returns** the pay left over after 20% tax has been taken out.

For example, working 10 hours at $20 an hour earns $200, so `net_pay(10, 20)` should **return** `160.0`.
