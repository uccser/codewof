# Is In Range

Write a function `is_in_range(number, lower, upper)` that **returns** `True` if `number` is between `lower` and `upper` (inclusive), or `False` otherwise.

You can assume `lower` is always less than or equal to `upper`.
