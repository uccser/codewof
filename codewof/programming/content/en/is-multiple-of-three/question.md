# Is Multiple of Three

Write a function `is_multiple_of_three(number)` that takes a number and **returns** `True` if it is a multiple of 3, or `False` otherwise.

*Hint: the `%` (modulo) operator is useful for finding out if a number is divisible by another number.*
