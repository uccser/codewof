# Times Table

Write a function `times_table(number)` that **prints** the first five multiples of `number` in the following format:

```
3 x 1 = 3
3 x 2 = 6
3 x 3 = 9
3 x 4 = 12
3 x 5 = 15
```

*Hint: the `str` function might be useful, e.g. `str(42)` is "42".*
