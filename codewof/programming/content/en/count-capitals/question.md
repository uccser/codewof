# Count Capitals

Write a function `count_capitals(text)` that takes a string, `text`, and **returns** the number of uppercase letters in the string.

*Hint: the string method `str.isupper()` will be useful.*
