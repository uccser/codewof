# Squares List

Write a function `squares(numbers)` that takes a Python list of numbers, `numbers`, and **returns** a new list where every number has been squared.

For example, `squares([1, 2, 3])` should **return** `[1, 4, 9]`.
