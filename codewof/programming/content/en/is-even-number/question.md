# Is Even?

Write a function `is_even(number)` that takes a number and **returns** `True` if the number is even, or `False` if it is odd.

The boolean value should be **returned**, not printed.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is even.*
