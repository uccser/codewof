# Is Even Number

Write a function `is_even(number)` that takes a number and **returns** `True` if the number is even, or `False` otherwise.

*Hint: the `%` (modulo) operator is useful when figuring out if a number is even.*
