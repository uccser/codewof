# Capitalise Word

Write a function `capitalise(word)` that takes a string, `word`, and **returns** the word with the first letter uppercase and all other letters lowercase.

*Hint: the string method `str.capitalize()` will be useful.*
