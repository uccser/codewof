# Absolute Difference

Write a function `difference(a, b)` that takes two numbers and **returns** the difference between them as a positive number.

For example, `difference(3, 10)` and `difference(10, 3)` should both **return** `7`.
