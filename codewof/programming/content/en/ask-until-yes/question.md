# Ask Until Yes

Write a program that repeatedly asks the user a question until they answer `yes`.

Each time the answer is not `yes`, **print** `Please say yes`. Once the user answers `yes`, **print** `Thanks!` and stop.
