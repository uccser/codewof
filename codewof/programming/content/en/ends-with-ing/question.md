# Ends With ing

Write a function `ends_with_ing(word)` that takes a string, `word`, and **returns** `True` if the word ends with `ing` (in any capitalisation), or `False` otherwise.

*Hint: the string methods `str.lower()` and `str.endswith()` may be useful.*
