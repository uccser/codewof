# Count Vowels

Write a program that asks for a word, then **prints** how many vowels (a, e, i, o, u) are in that word.

Capital letters should also be counted as vowels.

*Hint: the string method* `str.lower()` *may be useful here.*
