# Shortest Word

Write a function `shortest_word(words)` that takes a Python list of strings, `words`, and **returns** the shortest string in the list.

If two strings are the same length, **return** the one that appears first.

Your function will not be tested with an empty list.
