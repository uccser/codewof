# Cube Volume

Write a function `cube_volume(side_length)` that **returns** the volume of a cube with sides of length `side_length`.

The formula for the volume of a cube is v = s<sup>3</sup> where `v` is the volume and `s` is the length of a side.
