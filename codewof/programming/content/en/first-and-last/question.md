# First and Last

Write a function `first_and_last(text)` that takes a non-empty string `text` and **returns** a new string made from the first and last characters of `text`.

For example, `first_and_last("python")` should **return** `"pn"`.
