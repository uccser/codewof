# Loopy Loop

Here is a function that takes in a list of number and for each 
number in the list, prints out a string of '*' the length of the number.
Currently, we have a program that runs and uses a while loop to 
loop through the list. This is not the only way this can be done...
modify the given code so that it does not use a while loop but still
does the same thing and passes the tests. 
