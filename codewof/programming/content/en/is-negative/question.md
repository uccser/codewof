# Is Negative

Write a function `is_negative(number)` that takes a number and **returns** `True` if the number is less than 0, or `False` otherwise.

The boolean value should be **returned**, not printed.
