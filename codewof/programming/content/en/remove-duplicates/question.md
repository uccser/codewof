# Remove Duplicates

Write a function `remove_duplicates(items)` that takes a Python list, `items`, and **returns** a new list where each item only appears once.

The items should stay in the order that they first appear in the original list.
