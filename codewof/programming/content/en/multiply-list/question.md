# Multiply List

Write a function `product(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the result of multiplying all of the numbers together.

An empty list should **return** `1`.
