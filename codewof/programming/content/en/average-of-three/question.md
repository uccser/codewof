# Average of Three

Write a function `average(a, b, c)` that takes three numbers and **returns** their average (mean).

The average is the sum of the numbers divided by how many numbers there are.

*Note: You do not need to worry about the number of decimal places.*
