# Last Word

Write a function `last_word(sentence)` that takes a string, `sentence`, and **returns** the last word in that sentence.

Words are separated by spaces. Your function will not be tested with an empty string.

*Hint: the* `str.split()` *method breaks a sentence up into a list of words.*
