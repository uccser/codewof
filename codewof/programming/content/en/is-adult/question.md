# Is Adult

Write a function `is_adult(age)` that takes a number, `age`, and **returns** `True` if the person is 18 or older, or `False` otherwise.

The boolean value should be **returned**, not printed.
