# Countdown by Two

Write a function `countdown_by_two(start)` that uses a while loop to **print** `start`, then `start - 2`, then `start - 4` and so on, while the number is still greater than 0.

When the countdown is finished it should **print** `Liftoff!`
