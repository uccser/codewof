# Biggest Until Zero

Write a program that repeatedly asks for positive whole numbers until the user enters `0`.

Then your program should **print** the largest number that was entered.

*Hint: a* `while` *loop is a good type of loop to use when you don't know how many times the loop will run.*
