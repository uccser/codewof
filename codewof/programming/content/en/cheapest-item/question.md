# Cheapest Item

Write a function `cheapest(prices)` that takes a list of numbers, `prices`, and **returns** the smallest price in the list.

Your function will not be tested with an empty list.

*Hint: the built in `min()` function may come in handy.*
