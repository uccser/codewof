# First and Last Letter

Write a function `first_and_last(word)` that takes a string, `word`, and **returns** a new string made up of the first and last letters of `word`.

For example, `first_and_last('kiwi')` should **return** `'ki'`.

Your function will not be tested with an empty string.
