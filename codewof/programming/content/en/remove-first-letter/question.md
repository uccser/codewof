# Remove First Letter

Write a function `remove_first(text)` that takes a string, `text`, and **returns** the string without its first character.

*Hint: string slicing, e.g. `text[1:]`, may be useful.*
