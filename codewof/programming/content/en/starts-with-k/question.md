# Starts With K

Write a function `starts_with_k(word)` that takes a string, `word`, and **returns** `True` if the word starts with the letter K (uppercase or lowercase), or `False` otherwise.

*Hint: the string methods `str.lower()` and `str.startswith()` may be useful.*
