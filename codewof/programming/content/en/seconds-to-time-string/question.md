# Seconds to Time String

Write a function `time_string(total_seconds)` that takes a number of seconds and **returns** a string in the form `{minutes}m {seconds}s`.

For example, `time_string(90)` should **return** `'1m 30s'`.

*Hint: the `//` (floor division) and `%` (modulo) operators may be useful.*
