# Remove Vowels

Write a function `remove_vowels(text)` that takes a string, `text`, and **returns** a new string with all the vowels (a, e, i, o, u) removed.

Capital vowels should also be removed.

*Hint: the string method* `str.lower()` *may be useful here.*
