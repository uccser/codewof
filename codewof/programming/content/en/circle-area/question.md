# Circle Area

Write a function `circle_area(radius)` that **returns** the area of a circle with the given radius.

Use `3.14` as the value of pi. The formula is `area = pi * radius * radius`.

*Note: You do not need to worry about the number of decimal places.*
