# Star Rating

Write a function `star_rating(stars)` that takes a whole number, `stars`, and **returns** a string made up of that many `*` characters.

For example, `star_rating(3)` should **return** `'***'`.
