# Hui Tables

You are setting up tables for a hui. Each table seats 8 guests.

Write a program that asks how many guests are coming, then **prints** how many tables are needed so that every guest has a seat.

*Hint: the `%` (modulo) operator and the `//` (floor division) operator may be useful.*
