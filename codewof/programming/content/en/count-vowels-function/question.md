# Count Vowels in Text

Write a function `count_vowels(text)` that takes a string, `text`, and **returns** how many vowels (a, e, i, o, u) are in the string.

Capital letters also count as vowels.

*Hint: the string method* `str.lower()` *may be useful here.*
