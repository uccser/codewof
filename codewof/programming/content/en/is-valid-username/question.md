# Is Valid Username

Write a function `is_valid_username(name)` that takes a string, `name`, and **returns** `True` if the username is between 3 and 12 characters long (inclusive), or `False` otherwise.

The boolean value should be **returned**, not printed.
