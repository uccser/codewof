# Last Two Letters

Write a function `last_two(word)` that takes a string, `word`, and **returns** the last two letters of that string.

Your function will only be tested with words that are at least two letters long.

*Hint: string slicing, e.g.* `text[-2:]`*, may be useful.*
