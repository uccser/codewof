# Add Question Mark

Write a function `make_question(sentence)` that takes a string, `sentence`, and **returns** the sentence with the first letter capitalised and a question mark `?` added to the end.

For example, `make_question('kia ora')` should **return** `'Kia ora?'`.

*Hint: the string method* `str.capitalize()` *may be useful here.*
