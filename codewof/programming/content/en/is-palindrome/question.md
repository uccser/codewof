# Is Palindrome?

Write a function `is_palindrome(text)` that takes a string `text` and **returns** `True` if the string reads the same forwards and backwards, or `False` otherwise.

You can assume `text` will always be lowercase with no spaces.

*Hint: the slice `text[::-1]` gives a reversed copy of a string.*
