# Is Palindrome

A palindrome is a word that reads the same forwards and backwards, like `kayak`.

Write a function `is_palindrome(text)` that takes a string, `text`, and **returns** `True` if it is a palindrome, or `False` otherwise.

You can assume `text` is all lowercase.
