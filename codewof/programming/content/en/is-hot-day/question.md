# Is It a Hot Day?

Write a function `is_hot(temperature)` that takes a temperature in degrees celsius and **returns** `True` if the temperature is 25 or higher, or `False` otherwise.

The boolean value should be **returned**, not printed.
