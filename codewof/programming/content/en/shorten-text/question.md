# Shorten Text

Write a function `shorten(text)` that takes a string `text` and **returns** a shortened version of it.

If the string is longer than 10 characters, **return** the first 10 characters followed by `...`.
Otherwise **return** the original string.
