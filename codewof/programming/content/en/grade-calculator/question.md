# Grade Calculator

Write a function `grade(score)` that takes a test score and **returns** the grade for that score as a string.

The grades are:

- `A` for a score of 80 or more
- `B` for a score of 65 or more
- `C` for a score of 50 or more
- `D` for any other score
