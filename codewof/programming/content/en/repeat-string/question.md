# Repeat String

Write a function `repeat_string(text, times)` that takes a string `text` and an integer `times`, and **returns** the string repeated that many times.

For example, `repeat_string("ab", 3)` should **return** `"ababab"`.
