# Check Username

Write a program that repeatedly asks the user for a username until they enter one that does not contain a space.

Each time the username contains a space it should **print** `Usernames cannot contain spaces!`.
Once a valid username is entered it should **print** `Welcome {username}!`

*Hint: you can check if a string contains a space with* `if " " in username:`
