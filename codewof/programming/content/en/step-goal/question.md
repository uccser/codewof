# Step Goal

Write a function `check_steps(steps)` that takes the number of steps a person has walked today.

- If they have walked 10000 steps or more, it should **print** `Goal reached!`
- Otherwise it should **print** `You need {x} more steps.` where `x` is how many steps are still needed.
