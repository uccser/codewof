# Wind Warning

Write a function `wind_warning(speed)` that takes a wind speed in km/h and **prints** a warning.

- If the speed is 100 or more, **print** `Severe gale`
- Otherwise, if the speed is 60 or more, **print** `Strong wind`
- Otherwise, **print** `Calm enough`
