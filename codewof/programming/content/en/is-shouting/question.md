# Is Shouting

Write a function `is_shouting(text)` that takes a string, `text`, and **returns** `True` if all the letters in the string are uppercase, or `False` otherwise.

*Hint: the string method `str.isupper()` will be useful.*
