# Max of Two

Write a function `max_of_two(a, b)` that takes two numbers and **returns** the larger of the two.

If the numbers are equal, **return** that number.

*The built in* `max()` *function isn't an option for this question!*
