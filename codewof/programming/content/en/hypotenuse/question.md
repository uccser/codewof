# Hypotenuse

Write a function `hypotenuse(a, b)` that takes the lengths of the two shorter sides of a right angled triangle and **returns** the length of the longest side.

The formula is `c = (a * a + b * b) ** 0.5`.
