# Shout Word

Write a function `shout(word)` that takes a string, `word`, and **returns** the string in UPPERCASE with an exclamation mark `!` added to the end.

*Hint: the string method* `str.upper()` *may be useful here.*
