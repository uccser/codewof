# Total of List

Build a function `total(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the sum of those numbers.

*The simplest solution isn't an option for this question!*
