# Uppercase List

Write a function `upper_all(words)` that takes a Python list of strings, `words`, and **returns** a new list containing each string in UPPERCASE.

*Hint: the string method* `str.upper()` *may be useful here.*
