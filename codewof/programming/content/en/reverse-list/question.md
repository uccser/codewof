# Reverse List

Write a function `reverse_list(items)` that takes a Python list, `items`, and **returns** a new list with the items in the opposite order.

For example, `reverse_list([1, 2, 3])` should **return** `[3, 2, 1]`.
