# Celsius to Kelvin

Write a function `celsius_to_kelvin(temperature)` that takes a temperature in degrees celsius and **returns** the temperature in kelvin.

The formula is `kelvin = celsius + 273.15`.
