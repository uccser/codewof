# Hello Repeat

Write a program that asks the user for a name and then a number, and **prints** `Hello {name}!` that many times (each on a new line).

*Hint: a `for` loop with the `range` function is a good choice when you know how many times the loop should run.*
