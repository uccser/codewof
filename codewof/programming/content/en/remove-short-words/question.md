# Remove Short Words

Write a function `remove_short_words(words)` that takes a Python list of strings, `words`, and **returns** a new list containing only the strings that are longer than 3 characters.
