# Is Multiple of Five

Write a function `is_multiple_of_five(number)` that takes a number and **returns** `True` if the number is a multiple of 5, or `False` otherwise.

*Hint: the `%` (modulo) operator is useful here.*
