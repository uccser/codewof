# Is Vowel

Write a function `is_vowel(letter)` that takes a single character string, `letter`, and **returns** `True` if it is a vowel (a, e, i, o, u) or `False` otherwise.

Capital letters should also count as vowels.

*Hint: the string method* `str.lower()` *may be useful here.*
