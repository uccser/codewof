# Count Vowels

Write a program that asks the user for a word, then **prints** how many vowels (a, e, i, o, u) are in the word.

Capital letters also count as vowels.

*Hint: the string method* `str.lower()` *may be useful here.*
