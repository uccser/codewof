# Number Range

Write a function `number_range(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the range of the list.

The range is the largest number minus the smallest number.

Your function will not be tested with an empty list.

*Hint: the built in functions `max()` and `min()` may be useful.*
