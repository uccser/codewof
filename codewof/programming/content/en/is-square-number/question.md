# Is Square Number

Write a function `is_square_number(number)` that takes a whole number and **returns** `True` if it is a square number (like 1, 4, 9, 16) or `False` otherwise.

*Hint: `number ** 0.5` gives the square root of a number, and `int()` rounds a decimal down.*
