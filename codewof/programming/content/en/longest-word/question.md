# Longest Word

Write a function `longest_word(words)` that takes a list of strings, `words`, and **returns** the longest word in the list.

If two words are the same length, **return** the one that comes first in the list.
Your function will not be tested with an empty list.
