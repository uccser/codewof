# Longest Word

Write a function `longest_word(words)` that takes a Python list of strings, `words`, and **returns** the longest string in the list.

If two strings are the same length, **return** the one that appears first.

Your function will not be tested with an empty list.
