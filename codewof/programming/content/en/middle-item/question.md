# Middle Item

Write a function `middle_item(items)` that takes a Python list with an odd number of items, `items`, and **returns** the item in the middle of the list.

*Hint: remember that lists index from 0, and the `//` (floor division) operator may be useful.*
