# Middle Item

Write a function `middle_item(items)` that takes a Python list, `items`, and **returns** the item in the middle of the list.

Your function will only be tested with lists that have an odd number of items.

*Hint: the `//` (floor division) operator rounds down to the closest whole number.*
