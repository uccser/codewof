# Word and Length

Write a program that asks the user for a word, then **prints** the word on one line and the number of characters in the word on the next line.

*Hint: the built in function* `len()` *may be useful here.*
