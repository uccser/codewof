# Print Ladder

Write a function `print_ladder(rungs)` that takes an integer, `rungs`, and **prints** the string `|---|` once for each rung, each on a new line.

If `rungs` is 0 or less, nothing should be printed.
