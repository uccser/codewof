# Valid Triangle

*Warning: Challenging Question!*

Write a function `is_valid_triangle(a, b, c)` that takes the lengths of three sides and **returns** `True` if they could form a triangle, or `False` otherwise.

Three lengths can form a triangle only if the sum of every pair of sides is greater than the remaining side.
