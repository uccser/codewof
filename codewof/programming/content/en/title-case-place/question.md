# Title Case Place

Write a program that asks for the name of a place, then **prints** the name in title case (the first letter of each word capitalised).

*Hint: the string method* `str.title()` *may be useful here.*
