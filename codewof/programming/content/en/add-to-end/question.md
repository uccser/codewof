# Add to End

Write a function `add_to_end(items, item)` that takes a Python list, `items`, and a new item, `item`.

The function should add the item to the end of the list and then **return** the list.
