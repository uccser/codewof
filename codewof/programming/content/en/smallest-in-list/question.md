# Smallest in List

Write a function `smallest(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the smallest number in the list.

Your function will not be tested with an empty list.
