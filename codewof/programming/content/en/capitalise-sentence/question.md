# Capitalise Sentence

Write a program that asks for a sentence, then **prints** the sentence with only the first letter capitalised.

*Hint: the string method* `str.capitalize()` *may be useful here.*
