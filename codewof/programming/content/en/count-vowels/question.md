# Count Vowels - AI question

Write a function `count_vowels(text)` that takes a string `text` and **returns** the number of vowels (a, e, i, o, u) in the string.

For example, `count_vowels("hello")` should return 2.
