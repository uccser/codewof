# Last Three Characters

Write a function `last_three(text)` that takes a string, `text`, and **returns** the last three characters of the string.

If the string is shorter than three characters, **return** the whole string.

*Hint: string slicing, e.g. `text[-3:]`, may be useful.*
