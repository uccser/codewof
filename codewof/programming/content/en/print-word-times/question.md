# Print Word Times

Write a function `print_word_times(word, times)` that takes a string, `word`, and a whole number, `times`, and **prints** the word once per line, `times` times.

*Hint: a* `for` *loop with the* `range` *function is useful when you know how many times the loop will run.*
