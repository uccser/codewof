# Sum Even Numbers

Write a function `sum_evens(numbers)` that takes a Python list of numbers, `numbers`, and **returns** the sum of only the even numbers in the list.

*Hint: the `%` (modulo) operator is useful for finding even numbers.*
