# Percentage Score

Write a function `percentage(points, total)` that takes the points scored and the total points possible, and **returns** the score as a percentage.

For example, `percentage(3, 4)` should **return** `75.0`.
