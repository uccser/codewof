# Circle Circumference

Write a function `circumference(radius)` that **returns** the circumference of a circle with the given radius.

Use `3.14` as the value of pi. The formula is `circumference = 2 * pi * radius`.

*Note: You do not need to worry about the number of decimal places.*
