# Total Rainfall

A rain gauge records the rainfall in millimetres each day.

Write a function `total_rainfall(readings)` that takes a Python list of numbers, `readings`, and **returns** the total of all the readings that are greater than 0.
