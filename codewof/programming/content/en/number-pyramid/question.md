# Number Pyramid

Write a function `number_pyramid(rows)` that **prints** a pyramid of numbers with `rows` rows.

Row 1 contains the digit `1` once, row 2 contains the digit `2` twice, and so on.

For example, `number_pyramid(3)` should print:

```
1
22
333
```
