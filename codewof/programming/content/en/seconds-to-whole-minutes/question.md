# Seconds to Whole Minutes

Write a function `whole_minutes(seconds)` that takes a number of seconds and **returns** the number of whole minutes in that many seconds.

Any leftover seconds should be ignored.

*Hint: the `//` (floor division) operator rounds down to the closest whole number.*
