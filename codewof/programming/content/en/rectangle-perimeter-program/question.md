# Rectangle Perimeter Program

Write a program that asks for the width and then the length of a rectangle (both whole numbers), and **prints** the perimeter of the rectangle.

The perimeter of a rectangle is `2 * (width + length)`.
