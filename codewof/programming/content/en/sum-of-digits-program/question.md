# Sum of Digits

Write a program that asks for a positive whole number, then **prints** the sum of the digits of that number.

For example, if the input is `123` your program should **print** `6` because 1 + 2 + 3 = 6.

*Hint: you can loop over the characters of a string, and* `int()` *turns a character into a number.*
