# Fuel Economy

Write a function `fuel_economy(km, litres)` that takes the distance travelled in kilometres and the litres of fuel used, and **returns** the number of kilometres travelled per litre.

*Note: You do not need to worry about the number of decimal places; the number of decimal places is just the default when you return the value.*
