# Running Totals

Write a function `running_totals(numbers)` that takes a Python list of numbers, `numbers`, and **returns** a new list of the running totals.

For example, `running_totals([1, 2, 3])` should **return** `[1, 3, 6]` because the totals along the way are 1, then 1 + 2, then 1 + 2 + 3.
