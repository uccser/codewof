# Swap the Case

Write a function `swap_the_case(text)` that takes a string `text` and **returns** a new string where every uppercase letter is lowercase and every lowercase letter is uppercase.

*Hint: the string method `str.swapcase()` will be useful.*
