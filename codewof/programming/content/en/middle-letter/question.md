# Middle Letter

Write a function `middle_letter(word)` that takes a string, `word`, and **returns** the letter in the middle of the word.

Your function will only be tested with words that have an odd number of letters.

*Hint: the `//` (floor division) operator rounds down to the closest whole number.*
