# Count Kiwis

A conservation team records every bird they hear on a night walk.

Write a function `count_kiwis(birds)` that takes a Python list of strings, `birds`, and **returns** how many times the string `kiwi` appears in the list.

The strings in `birds` will usually be lowercase.
