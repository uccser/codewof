# Library Fine

Write a function `fine_in_cents(days_late)` that **returns** the fine for a late library book in cents.

- The first 3 days late are free.
- After that, each late day costs 25 cents.

For example, `fine_in_cents(5)` should **return** `50`.
