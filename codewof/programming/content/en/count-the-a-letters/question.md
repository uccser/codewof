# Count the A Letters

Write a program that asks for a word, then **prints** how many times the letter `a` appears in it.

Capital `A` should also be counted.

*Hint: the string method* `str.lower()` *may be useful here.*
