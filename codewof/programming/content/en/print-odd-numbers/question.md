# Print Odd Numbers

Write a function `print_odds(limit)` that **prints** every odd number from 1 up to and including `limit`, each on a new line.

For example, `print_odds(7)` should print:

```
1
3
5
7
```
