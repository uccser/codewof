name: Auto-merge minor dependency updates

on: pull_request

permissions:
  pull-requests: write
  contents: write

jobs:
  check-pull-request:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' }}
    steps:
      - name: Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3.0.0
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
      - name: Approve
      # Python major versions are in the semver minor version position, we do NOT want to autoupdate these
        if: ${{!contains(steps.metadata.outputs.dependency-names, 'python') && steps.metadata.outputs.update-type == 'version-update:semver-minor' || steps.metadata.outputs.update-type == 'version-update:semver-patch'}}
        run: gh pr review --body "Automatic approval for minor dependency update." --approve "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
      - name: Merge
        # Python major versions are in the semver minor version position, we do NOT want to autoupdate these
        if: ${{!contains(steps.metadata.outputs.dependency-names, 'python') && steps.metadata.outputs.update-type == 'version-update:semver-minor' || steps.metadata.outputs.update-type == 'version-update:semver-patch'}}
        run: gh pr merge --auto --merge "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
